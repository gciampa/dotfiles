# Dotfiles

Personal dotfiles for macOS. Includes ZSH, Neovim, Git, Homebrew packages, and Claude Code configuration.

## Installation

```bash
git clone https://github.com/gciampa/dotfiles.git ~/dotfiles
cd ~/dotfiles
make install
```
